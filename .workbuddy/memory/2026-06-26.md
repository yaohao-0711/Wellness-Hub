# 2026-06-26

## 心理小屋 Wellness-Hub 项目

- 克隆仓库 yaohao-0711/Wellness-Hub 到工作目录
- 通读整个项目代码（index.html ~4500行，单文件 PWA）
- 讨论改进方案，用户否掉了数据管理（导出/导入/清除）、存储用量、每日提醒
- 用户同意做：个性化设置 + 关于页增强

### v1.1 实现内容（已提交 0df753d）

**个性化设置：**
- 欢迎弹窗开关（toggle-welcome，localStorage key: wellness_welcome_show）
- 自定义欢迎副标题（localStorage key: wellness_subtitle）
- 自定义语录（localStorage key: wellness_custom_quotes，与内置160+条混合轮播）

**关于页增强：**
- 版本更新日志（可展开，v1.0 + v1.1）
- GitHub 反馈入口（跳转 Issues）
- 开源致谢（Supabase、PWA、心理学名言出处等）

**技术细节：**
- 设置页 HTML 新增个性化区域 + 关于区域增强
- 新增 subtitle editor modal + custom quotes page
- getNextQuote() 改为合并内置+自定义语录
- init() 中欢迎弹窗受 getWelcomeShow() 控制
- setView() 中 view-custom-quotes 也隐藏 tab bar

### 删除反馈建议和开源致谢（commit 6bf35b8）
- 用户要求删掉"反馈建议"和"开源致谢"两个设置项

### v1.1+ 记录搜索、排序、日历打卡标记（commit cafc03d）

**记录页：**
- 搜索：topbar 加🔍按钮，点击展开搜索框，实时过滤+关键词高亮（.record-highlight）
- 排序：topbar 加↕按钮，切换正序/倒序，变量 recordSortAsc

**日历页：**
- renderCalendar() 中遍历 tasksCache 统计每天记录数 → dayRecordMap
- 有记录的日期加小圆点 + 背景高亮 + cursor:pointer
- openDayRecords(day) 弹出当天所有记录的 overlay

### v1.2 引力曲线升级、本周回顾、待办截止日期（commit 75cc255）

**引力曲线升级（11）：**
- renderMoodHistoryChart() 从方块条形图改为文字气泡时间线
- 显示最近7天有记录的天，今天高亮紫色左边框
- 新增 CSS：.gravity-timeline-item, .gravity-timeline-bubble, .gravity-timeline-today

**本周回顾（12）：**
- openGravityDetail() 顶部加"本周回顾"折叠卡片
- toggleWeekReview() 控制展开/折叠
- 展示最近7天期待列表

**待办截止日期（14）：**
- 新建待办 modal 加 date 输入框（todo-new-deadline）
- createTodo() 存储 deadline 字段
- renderTodos() 根据 deadline 计算状态：临期≤3天橙色、过期红色
- renderTodoDetailView() 显示截止日期状态，编辑时可修改
- saveTodoEdit() 保存 deadline
- CSS 新增：.todo-deadline, .todo-deadline-warning, .todo-deadline-overdue

### 待办临期提醒改为≤1天（commit f39f63d）
- renderTodos() 和 renderTodoDetailView() 中 diffDays <= 3 改为 <= 1

### v1.3 成就系统、页面动画、震动反馈（commit a16ce75）

**成就系统（16）：**
- 12个勋章，铜/银/金/钻石四级难度
- ACHIEVEMENTS 数组定义勋章规则，checkAchievements() 检查解锁
- renderMePage() 底部调用 renderAchievements()
- 记录保存和待办完成时自动 checkAchievements()
- localStorage key: wellness_achievements

**页面过渡动画（17）：**
- setView() 中新 view 添加 slide-in-right 动画（250ms）
- switchTab() 中 tab-view 添加 fade-in 动画（200ms）
- CSS @keyframes: slideInRight, slideInLeft, fadeTabIn

**震动反馈（19）：**
- hapticFeedback(pattern) 封装函数，受 getHapticEnabled() 控制
- 设置页外观区域新增"📳 震动反馈"开关
- 触发点：记录保存10ms、删除[10,50,10]ms、待办完成10ms、长按50ms、成就解锁[20,80,20]ms
